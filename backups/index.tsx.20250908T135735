// app/index.tsx
import LanguageModal from "@/components/LanguageModal"; // expects components/LanguageModal.tsx present
import { useLanguage } from "@/context/LanguageContext";
import React, { useState } from "react";
import { ScrollView, StyleSheet, Text, TouchableOpacity, View } from "react-native";

export default function Index() {
  const [modalVisible, setModalVisible] = useState(false);
  const { langName } = useLanguage();

  return (
    <View style={{ flex: 1 }}>
      <ScrollView contentContainerStyle={{ padding: 16 }}>
        <View style={styles.headerRow}>
          <Text style={styles.h1}>भगवद गीता</Text>

          {/* Language dropdown button (opens modal) */}
          <TouchableOpacity style={styles.langBtn} onPress={() => setModalVisible(true)}>
            <Text style={styles.langText}>{langName} ▾</Text>
          </TouchableOpacity>
        </View>

        {/* Hero / description */}
        <View style={styles.hero}>
          {/* keep your hero content; example image area */}
          <View style={styles.heroImagePlaceholder} />
          <Text style={styles.heroTitle}>भगवद गीता</Text>
          <Text style={styles.heroDesc}>
            श्रीमद्भगवद्गीता (संस्कृत: श्रीमद्भगवद्गीता...) {/* shorten as needed */}
          </Text>
        </View>

        {/* Chapters grid / list — unchanged from your implementation */}
        <View style={styles.chapters}>
          {/* Example chapter tiles — replace with your mapped list */}
          <View style={styles.row}>
            <View style={styles.card}><Text style={styles.cardText}>Chapter 1 अर्जुन विवाद योग</Text></View>
            <View style={styles.card}><Text style={styles.cardText}>Chapter 2 Sankhya Yog</Text></View>
          </View>
          <View style={styles.row}>
            <View style={styles.card}><Text style={styles.cardText}>Chapter 3 Karma Yog</Text></View>
            <View style={styles.card}><Text style={styles.cardText}>Chapter 4 ज्ञान कर्म</Text></View>
          </View>
        </View>
      </ScrollView>

      <LanguageModal visible={modalVisible} onClose={() => setModalVisible(false)} />
    </View>
  );
}

const styles = StyleSheet.create({
  headerRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 12 },
  h1: { fontSize: 24, fontWeight: "800" },
  langBtn: {
    paddingVertical: 6,
    paddingHorizontal: 12,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: "#e0e0e0",
    backgroundColor: "#fff",
  },
  langText: { fontSize: 14, fontWeight: "600" },

  hero: { marginVertical: 12 },
  heroImagePlaceholder: { height: 150, borderRadius: 10, backgroundColor: "#ddd", marginBottom: 12 },
  heroTitle: { fontSize: 20, fontWeight: "700" },
  heroDesc: { marginTop: 8, color: "#444" },

  chapters: { marginTop: 20 },
  row: { flexDirection: "row", justifyContent: "space-between", marginBottom: 18 },
  card: {
    flex: 0.48,
    height: 110, // adjust tile height here
    backgroundColor: "#fff",
    borderRadius: 10,
    justifyContent: "center",
    padding: 12,
    shadowColor: "#000",
    shadowOpacity: 0.03,
    shadowRadius: 6,
  },
  cardText: { fontWeight: "700", fontSize: 14 },
});
